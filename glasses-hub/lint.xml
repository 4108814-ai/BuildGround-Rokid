<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!-- Current SDK annotations expect LineBreaker constants; these untouched
         renderers use the equivalent legacy Layout constants and are outside slice 2. -->
    <issue id="WrongConstant">
        <ignore path="src/main/java/com/anezium/rokidbus/glasses/MediaHudView.kt" />
        <ignore path="src/main/java/com/anezium/rokidbus/glasses/NoticeOverlayRenderer.kt" />
        <ignore path="src/main/java/com/anezium/rokidbus/glasses/ReaderSurfaceView.kt" />
    </issue>

    <!-- Required by the YodaOS self-arm path; deployment grants it out of band. -->
    <issue id="ProtectedPermissions">
        <ignore path="src/main/AndroidManifest.xml" />
    </issue>

    <!-- The check only fires now that Lottie pulls appcompat onto the classpath;
         this pre-existing plain ImageView never ran under an AppCompat theme. -->
    <issue id="AppCompatCustomView">
        <ignore path="src/main/java/com/anezium/rokidbus/glasses/NoticeOverlayRenderer.kt" />
    </issue>
</lint>
