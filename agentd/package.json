{
  "name": "nexus-agentd",
  "version": "0.1.0",
  "description": "PC-side monitoring daemon for Nexus Agents mission control",
  "license": "MIT",
  "private": true,
  "type": "commonjs",
  "main": "dist/cli.js",
  "bin": {
    "agentd": "dist/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/*.test.js",
    "smoke": "npm run build && node test/smoke.js"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/qrcode-terminal": "^0.12.2",
    "@types/ws": "^8.18.1",
    "typescript": "^5.8.3"
  }
}
